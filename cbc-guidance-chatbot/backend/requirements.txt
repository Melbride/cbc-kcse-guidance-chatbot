fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
python-multipart==0.0.6
pydantic==2.7.4
numpy==1.26.4
psycopg2-binary==2.9.9
pypdf==4.0.1
python-docx==0.8.11
requests==2.31.0

# Pinecone
pinecone-client==5.0.1

# LangChain
langchain==0.2.0
langchain-core==0.2.43
langchain-community==0.2.0
langchain-text-splitters==0.2.4
langchain-pinecone==0.1.3
langsmith==0.1.147

# LLM - Groq
groq==0.9.0
langchain-groq==0.1.9

# REMOVED: sentence-transformers, transformers, langchain-huggingface
# These pull in torch (~800MB) and exceed Render free tier memory.
# Embeddings are now handled via HuggingFace Inference API using
# the HUGGINGFACEHUB_API_TOKEN env var — no local model needed.
